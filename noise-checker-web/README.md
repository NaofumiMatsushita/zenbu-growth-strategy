# ZENBU騒音チェッカー Web版

管理会社向けダッシュボード・管理システム（React + TypeScript + Material-UI）

## 📋 概要

ZENBU騒音チェッカーのWeb版は、不動産管理会社向けの騒音管理ダッシュボードです。複数物件の騒音データを一元管理し、効率的なトラブル対応を実現します。

## 🎯 主要機能

### 1. ダッシュボード
- 全物件の騒音状況一覧表示
- リアルタイムアラート表示
- 未対応クレーム件数の可視化
- 週次/月次サマリーグラフ

### 2. 物件管理
- 物件の登録・編集・削除
- 物件情報の一括インポート（CSV）
- 物件ごとの騒音履歴表示
- 部屋別の騒音マップ

### 3. 音声アップロード・分析
- ドラッグ&ドロップでファイルアップロード
- 対応形式: WAV, MP3, M4A, AAC
- 自動騒音分析（dB計算、判定）
- 分析結果の即時表示

### 4. データ管理
- 測定データ一覧・検索
- 期間、物件、dB範囲でフィルタ
- CSV/Excel一括エクスポート
- 詳細レポート生成

### 5. 統計・分析
- 物件別騒音傾向分析
- 時間帯別分布グラフ
- 部屋別ランキング
- トレンド分析（月次推移）

### 6. ユーザー管理
- 管理者・担当者・閲覧者の権限管理
- チームメンバーの招待
- ログイン履歴の確認

## 🛠 技術スタック

### フロントエンド
- **フレームワーク**: React 18.2
- **言語**: TypeScript 5.3
- **ビルドツール**: Vite 5.0
- **UIライブラリ**: Material-UI (MUI) 5.15
- **ルーティング**: React Router 6.21
- **グラフ**: Chart.js 4.4 + react-chartjs-2
- **HTTP**: Axios 1.6
- **日付処理**: date-fns 3.2
- **ファイル処理**: react-dropzone 14.2, xlsx 0.18

### バックエンド（別途実装）
- **API**: Node.js (Express) or Python (FastAPI)
- **データベース**: PostgreSQL + Redis
- **ストレージ**: AWS S3 or Google Cloud Storage
- **認証**: JWT

## 📂 プロジェクト構造

```
noise-checker-web/
├── package.json              # 依存関係
├── tsconfig.json             # TypeScript設定
├── vite.config.ts            # Vite設定
├── index.html                # エントリーHTML
├── public/                   # 静的ファイル
└── src/
    ├── main.tsx              # エントリーポイント
    ├── App.tsx               # メインアプリ
    ├── components/           # 再利用可能コンポーネント
    │   ├── Dashboard/        # ダッシュボード関連
    │   ├── Properties/       # 物件管理関連
    │   ├── Upload/           # アップロード関連
    │   ├── Data/             # データ管理関連
    │   ├── Statistics/       # 統計関連
    │   └── Common/           # 共通コンポーネント
    ├── pages/                # ページコンポーネント
    │   ├── DashboardPage.tsx
    │   ├── PropertiesPage.tsx
    │   ├── UploadPage.tsx
    │   ├── DataPage.tsx
    │   └── StatsPage.tsx
    ├── hooks/                # カスタムフック
    ├── utils/                # ユーティリティ関数
    ├── types/                # TypeScript型定義
    ├── services/             # APIサービス
    └── styles/               # グローバルスタイル
```

## 🚀 セットアップ

### 前提条件

- Node.js 18以上
- npm or yarn

### インストール

```bash
cd noise-checker-web
npm install
```

### 開発サーバー起動

```bash
npm run dev
```

ブラウザで `http://localhost:3000` が自動的に開きます。

### ビルド

```bash
npm run build
```

ビルドされたファイルは `dist/` ディレクトリに出力されます。

### プレビュー

```bash
npm run preview
```

## 📱 画面構成

### 1. ログイン画面
- メールアドレス + パスワード
- Google SSO対応（オプション）

### 2. ダッシュボード（トップ画面）

```
┌─────────────────────────────────────────────┐
│ ZENBU騒音チェッカー          [通知] [👤]   │
├─────────────────────────────────────────────┤
│ 📊 ダッシュボード                            │
│                                             │
│ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐      │
│ │登録  │ │今月  │ │警告  │ │未対応│      │
│ │物件  │ │測定  │ │中    │      │      │
│ │ 48   │ │1,234 │ │  3   │ │  7   │      │
│ └──────┘ └──────┘ └──────┘ └──────┘      │
│                                             │
│ ■ 物件一覧        [ 🔍検索 ] [ +新規登録 ]│
│ ┌─────────────────────────────────────┐    │
│ │ 🟢 ○○マンション (渋谷区)           ►│    │
│ │ 🟠 △△アパート (新宿区)             ►│    │
│ │ 🔴 □□ハイツ (世田谷区)             ►│    │
│ └─────────────────────────────────────┘    │
│                                             │
│ ■ 週次サマリー                              │
│ [グラフ表示]                                │
└─────────────────────────────────────────────┘
```

### 3. 物件管理画面
- 物件一覧（カード or リスト表示）
- 物件詳細（基本情報、騒音履歴、部屋別データ）
- 物件登録・編集フォーム
- CSVインポート機能

### 4. 音声アップロード画面
- ドラッグ&ドロップエリア
- ファイル選択ボタン
- アップロード進捗表示
- 分析結果の即時表示

### 5. データ管理画面
- 測定データテーブル
- 高度な検索・フィルタ
- 一括操作（削除、ステータス変更）
- レポート生成

### 6. 統計・分析画面
- 物件別サマリーカード
- グラフ（時系列、円グラフ、棒グラフ）
- 部屋別ランキング
- データエクスポート

## 🎨 UI/UXの特徴

### Material-UI (MUI)使用

- **レスポンシブ**: モバイル・タブレット・PC対応
- **ダークモード**: ライト/ダークテーマ切替
- **アクセシビリティ**: ARIA属性、キーボード操作対応
- **カスタマイズ**: ブランドカラーに対応したテーマ

### 主要コンポーネント

- `AppBar`: ヘッダーナビゲーション
- `Drawer`: サイドバーメニュー
- `Card`: 情報カード
- `DataGrid`: データテーブル
- `Dialog`: モーダル
- `Snackbar`: 通知

## 🔒 セキュリティ

### 実装予定

- JWT認証
- HTTPS通信
- XSS対策（React標準）
- CSRF対策
- 入力値のバリデーション
- ファイルアップロードの検証
- アクセス権限の制御

## 📊 データフロー

```
ユーザー操作
    ↓
React Component
    ↓
Custom Hook
    ↓
API Service (Axios)
    ↓
Backend API
    ↓
Database / Storage
```

## 🧪 テスト（今後実装）

```bash
# ユニットテスト
npm run test

# E2Eテスト
npm run test:e2e

# カバレッジ
npm run test:coverage
```

## 📦 デプロイ

### Vercel

```bash
npm install -g vercel
vercel
```

### Netlify

```bash
npm run build
netlify deploy --prod --dir=dist
```

### AWS S3 + CloudFront

```bash
npm run build
aws s3 sync dist/ s3://your-bucket-name
```

## 🔧 環境変数

`.env.local` を作成:

```env
# API URL
VITE_API_URL=http://localhost:8000/api

# 認証
VITE_AUTH_DOMAIN=your-auth-domain
VITE_AUTH_CLIENT_ID=your-client-id

# ストレージ
VITE_STORAGE_URL=https://your-storage.com

# Google Analytics
VITE_GA_ID=GA-XXXXXXXXX
```

## 🎯 開発ロードマップ

### Phase 1: MVP（現在）
- ✅ プロジェクトセットアップ
- ✅ 基本レイアウト
- ⏳ ダッシュボード実装
- ⏳ 物件管理実装
- ⏳ アップロード実装

### Phase 2: 機能拡張
- ⏳ データ管理
- ⏳ 統計・分析
- ⏳ ユーザー管理
- ⏳ アラート・通知

### Phase 3: 高度化
- ⏳ AI音源分類連携
- ⏳ 入居者ポータル連携
- ⏳ 外部システム連携（API）
- ⏳ モバイルアプリ連携

## 📝 実装サンプル

### ダッシュボードコンポーネント例

```typescript
// src/pages/DashboardPage.tsx
import React from 'react';
import { Box, Grid, Card, CardContent, Typography } from '@mui/material';

export const DashboardPage: React.FC = () => {
  return (
    <Box sx={{ flexGrow: 1, p: 3 }}>
      <Typography variant="h4" gutterBottom>
        ダッシュボード
      </Typography>

      <Grid container spacing={3}>
        {/* 統計カード */}
        <Grid item xs={12} sm={6} md={3}>
          <Card>
            <CardContent>
              <Typography color="textSecondary" gutterBottom>
                登録物件
              </Typography>
              <Typography variant="h3">
                48
              </Typography>
            </CardContent>
          </Card>
        </Grid>

        {/* 他の統計カード */}
      </Grid>

      {/* 物件一覧 */}
      {/* グラフ */}
    </Box>
  );
};
```

### API呼び出し例

```typescript
// src/services/api.ts
import axios from 'axios';

const API_BASE_URL = import.meta.env.VITE_API_URL;

const api = axios.create({
  baseURL: API_BASE_URL,
  headers: {
    'Content-Type': 'application/json',
  },
});

// リクエストインターセプター（認証トークン追加）
api.interceptors.request.use((config) => {
  const token = localStorage.getItem('token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

export const propertyService = {
  getAll: () => api.get('/properties'),
  getById: (id: string) => api.get(`/properties/${id}`),
  create: (data: any) => api.post('/properties', data),
  update: (id: string, data: any) => api.put(`/properties/${id}`, data),
  delete: (id: string) => api.delete(`/properties/${id}`),
};

export const measurementService = {
  getAll: (params?: any) => api.get('/measurements', { params }),
  upload: (file: File, metadata: any) => {
    const formData = new FormData();
    formData.append('file', file);
    formData.append('metadata', JSON.stringify(metadata));
    return api.post('/measurements/upload', formData, {
      headers: { 'Content-Type': 'multipart/form-data' },
    });
  },
};
```

## 🐛 トラブルシューティング

### ポート3000が使用中

```bash
# 別のポートを指定
npm run dev -- --port 3001
```

### ビルドエラー

```bash
# node_modulesをクリーンアップ
rm -rf node_modules package-lock.json
npm install
```

### Material-UIのスタイルが適用されない

`src/main.tsx` で `@mui/material/styles` の `ThemeProvider` を確認

## 📚 参考リンク

- [React公式](https://react.dev/)
- [TypeScript公式](https://www.typescriptlang.org/)
- [Vite公式](https://vitejs.dev/)
- [Material-UI](https://mui.com/)
- [React Router](https://reactrouter.com/)
- [Chart.js](https://www.chartjs.org/)

## 👥 開発者

**ZENBU株式会社**
- Website: https://all-zenbu.co.jp
- Email: info@all-zenbu.co.jp
- TEL: 052-332-6770

## 📄 ライセンス

Copyright © 2026 ZENBU株式会社

---

**バージョン**: 1.0.0
**最終更新**: 2026年1月23日

## 🚧 開発状況

| 機能 | 状態 | 備考 |
|------|------|------|
| プロジェクトセットアップ | ✅ 完了 | package.json, tsconfig等 |
| ダッシュボード | 🔨 開発中 | 基本レイアウトまで |
| 物件管理 | ⏳ 未実装 | - |
| アップロード | ⏳ 未実装 | - |
| データ管理 | ⏳ 未実装 | - |
| 統計・分析 | ⏳ 未実装 | - |
| ユーザー管理 | ⏳ 未実装 | - |
| バックエンドAPI | ⏳ 未実装 | 別途開発が必要 |

## 次のステップ

1. **src/配下のコンポーネント実装**
   - main.tsx
   - App.tsx
   - 各ページコンポーネント

2. **バックエンドAPIの開発**
   - Node.js (Express) or Python (FastAPI)
   - PostgreSQL設定
   - 認証機能

3. **統合テスト**
   - フロントエンド + バックエンド連携

4. **本番デプロイ**
   - フロントエンド: Vercel / Netlify
   - バックエンド: AWS / GCP
